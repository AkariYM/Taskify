<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.taskifya">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.TaskifyA">

        <!-- ACTIVIDAD PRINCIPAL -->
        <activity
            android:name=".personalizacion.PersonalizacionActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ACTIVIDADES DE PERSONALIZACIÓN -->
        <activity android:name=".personalizacion.PersonalizacionTemaActivity" android:exported="true"/>
        <activity android:name=".personalizacion.PersonalizacionNotificacionesActivity" android:exported="true"/>
        <activity android:name=".personalizacion.PersonalizacionAyudaRapidaActivity" android:exported="true"/>
        <activity android:name=".personalizacion.PersonalizacionCopiaDeSeguridadActivity" android:exported="true"/>

        <!-- MAIN ACTIVITY (COMPOSE) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.TaskifyA" />

    </application>

</manifest>
